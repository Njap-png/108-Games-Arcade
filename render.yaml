services:
  - type: web
    name: 108-games-arcade
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: serve -s . -l ${PORT:-10000}
    healthCheckPath: /
    autoDeploy: true